First remember to do the following:
1) npm i @react-navigation/native @react-navigation/stack
2) In the ./utils/api.js, make sure to update the db using this routes: /login, /signup, /users/${user.id}
3) Implement the ./actions/types.js
4) Update the code with more styles, ui, functions and widgets
5) Test build the app
6) Host our backend so that our app can fetch it
7) Finish all and post to github, then package web to render.com and build the app using eas